#include "SourceTokenizer.h"

SourceTokenizer::SourceTokenizer(std::istream* stream) : Tokenizer(stream) {}

std::vector<Token*> SourceTokenizer::tokenize() {
    // implementation
    std::vector<Token*> tokenList;
    return tokenList;
}